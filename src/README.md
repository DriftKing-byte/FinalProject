# src/

Put reusable R scripts (e.g., data loading, modeling functions) here.

#Do this first to see which files have changed
git status

#Then, do this to add everything
git add .

#For committing a message
git commit -m "Write a clear message about what you changed"

#Then push
git push origin main

